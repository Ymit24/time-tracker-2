services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: time-tracker-2
    restart: unless-stopped
    ports:
      - "3000:80"
    environment:
      - PORT=80
      - HOST=_
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
